# AGENTS.md — Agent 工作铁律

## 严禁：工具调用写成纯文本标签（XML-as-text bug）
**绝对禁止**把工具调用敲成展示用的纯文本标签（如把 `invoke`/`antml` 标签当普通输出打印），
而不真正发起工具调用。这会导致"说了要做却没做"、回合凭空中断。

- **触发模式**：先写一句旁白（"我先…然后…"/"先把…查清"），紧跟的【首个工具调用】被误渲染成文本。
- **根因**：预告式旁白 + 紧随其后的工具动作，在接缝处被写成标签文本而非真实调用。
- **纠正**：少写预告式旁白；要执行就【直接发起真正的工具调用】，不要先用文字描述「我即将调用 X」。
  若必须说明意图，把说明放在工具调用【之后】或与调用分离，绝不在调用位置打印标签文本。
