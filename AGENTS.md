# AGENTS.md

## CRITICAL: 工具调用不得泄漏成文本（反复发生的故障）

**故障现象**: 助手在一轮回复里把工具调用写成了纯文本（形如 `call` 紧跟一段调用结构标签）而非真正发起调用。结果该工具根本没执行，任务状态不变，对话"卡住""中断"，用户被迫反复说"继续/jixu"。曾在会话 `llm_vlm_mid` 反复出现十余次。

**根因**: 上下文里出现"长得像工具调用的字面标签文本"会诱导模型把真调用也写成文本。本项目尤其高发，因为对话里常有计划文件、会话转录、代码示例、含占位符的 prompt 文件，里面带这类标签串。**写完含占位符（如 `{xxx}`、`{{...}}`）或大段文本的文件后，紧接着发长参数工具调用最易诱发。**

**铁律**:
1. 正文（给用户看的文本）里**绝不**书写工具调用的结构标签字面串——即使是为了解释这个 bug、举例、引用转录，也只用自然语言描述，不贴原串。
2. 引用旧会话/计划/转录时，先用脚本提取摘要再看结论，**不整段回灌**含标签或占位符的原始内容到主上下文。
3. 每次发起改状态的工具（任务状态更新、git 提交等）后，**下一步先确认它真的生效**（状态真变了/commit 真产生），没生效就当轮补发，不靠用户催。
4. 实质动作直接做，过渡句尽量短；含占位符的文件落盘后，下一个工具调用单独、简短地发，不与叙述性长文本连在一起。
5. **首选用 subagent 执行含占位符/大段代码的任务**：子代理在隔离上下文里干活，把诱发性内容挡在主上下文之外，是对该故障最有效的结构性防护。
